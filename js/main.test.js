const {deleteBlocks} = require('./main.js')
const Victor = require('Victor')

test('deleteBlocksWithNoHealth', () => {
    test('should delete one empty block', () => {
        expect(deleteBlocks)
    })
})